[project]
name = "sqlite-faker"
version = "0.1.0"
description = "Generate SQLite databases with fake data using Faker library"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "faker>=39.0.0",
]
